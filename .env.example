# Listener
LISTEN_ADDR="0.0.0.0"
LISTEN_PORT="5060"

# Optional: if behind NAT, publish this (omit or leave empty if not needed)
ADVERTISE_ADDR=""

# Targets
F1_HOST="f1.emaktalk.com"
F2_HOST="f2.emaktalk.com"

# Comma-separated allowlist of carrier source IPs or CIDRs (no spaces or with — both fine)
# Example: "198.51.100.10,198.51.100.11,203.0.113.0/24"
CARRIER_SOURCES="198.51.100.10,198.51.100.11,198.51.100.12,198.51.100.13,198.51.100.14"

# PostgREST (read-only)
POSTGREST_RO_DB_URL="https://prod-ro-db.emaktalk.com/api"
POSTGREST_KEY="REPLACE_ME"

# Your view path (no trailing ?). We will append `?select=num,target`
# Example: "/edge_export_routing_view"
POSTGREST_EXPORT_PATH="/edge_export_routing_view"

# How often to refresh routes
PULL_INTERVAL="2m"
